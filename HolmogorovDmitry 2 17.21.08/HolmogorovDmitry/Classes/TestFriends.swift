import UIKit

class TestFriend {
    var name: String
    var last_Name: String
    var avatar: UIImage
    
    init(name: String, last_Name: String, avatar: UIImage) {
        self.name = name
        self.last_Name = last_Name
        self.avatar = avatar
    }
}
